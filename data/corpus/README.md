# Corpus directory

Place your own documents here for indexing. This directory is empty by default.

- Drop in `.txt` files (the default format). One file = one document.
- The file name (without its extension) is stored in the document metadata.
- To support other formats, extend `indexing.py`.

Only index content you have the right to use, and do not include personal or
confidential data.
